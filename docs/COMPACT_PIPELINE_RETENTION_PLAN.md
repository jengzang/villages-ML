# Compact Pipeline Retention Plan

> For implementation agents: treat this as the source of truth for compact-mode table retention until it is superseded by a newer docs file. Do not implement destructive cleanup without tests and an explicit confirmation of this table list.

**Goal:** Define the compact pipeline database shape for national-scale village-name analysis.

**Scope:** Compact mode keeps global and regional analysis outputs, plus lightweight metadata and character similarity. It drops or skips village-level derived tables that scale with the number of natural villages and only support village-detail APIs.

**Decision:** `char_similarity` is retained in compact mode.

---

## Compact Definition

Compact mode is an analysis-result database, not a full village-detail database.

It should support:

- Character frequency, tendency, significance, embeddings, and character similarity/network APIs.
- Semantic category, subcategory, and regional index APIs.
- N-gram frequency, tendency, significance, and structural pattern APIs.
- Morphology pattern frequency and regional tendency APIs.
- Region similarity and region clustering APIs.
- Spatial hotspot and regional spatial summary APIs.
- Pipeline metadata, active run selection, and query-policy metadata.

It does not need to support:

- Per-village n-gram lookup.
- Per-village semantic structure lookup.
- Per-village feature vectors.
- Per-village spatial feature/detail lookup.
- Village-detail API payloads that include ngrams, semantic structure, spatial features, and feature vectors.
- Online subset compute flows that require `village_features`.

## Splitting Logic

Use these rules when deciding whether a table belongs in compact mode.

1. **Granularity rule**

   Keep tables where one row represents a global result, region result, character, semantic category, n-gram, pattern, cluster, hotspot, or run.

   Drop tables where one row represents a natural village or a natural-village-to-feature assignment.

2. **API capability rule**

   Compact keeps APIs for regional/global analysis. Compact does not promise village-detail APIs.

3. **Final-result rule**

   Keep final analysis outputs. Drop intermediate tables if the backend does not need to query them after the pipeline completes.

4. **Size-risk rule**

   Tables scaling as `villages`, `villages x features`, or `villages x assignments` are first-class cleanup targets. Tables scaling as `characters x top_k` are acceptable if they support active APIs.

5. **Character similarity exception**

   `char_similarity` is a precomputed character-level result, not a village-level table. It is required by current backend character similarity and character network APIs, so compact mode keeps it.

## Tables To Keep

These tables must exist in compact mode when their generating phases are selected.

### Run And Metadata

- `active_run_ids`
- `analysis_runs`
- `embedding_runs`
- `query_policy_config`
- `query_logs`

### Character Analysis

- `char_frequency_global`
- `char_regional_analysis`
- `tendency_significance`
- `char_embeddings`
- `char_similarity`

Rationale for `char_similarity`: it is generated by Phase 1 when `--precompute-similarities` is enabled, and the backend reads it directly for `/character/embeddings/similarities` and `/character/network`.

### Semantic Analysis

- `semantic_vtf_global`
- `semantic_regional_analysis`
- `semantic_indices`
- `semantic_indices_detailed`
- `semantic_subcategory_vtf_global`
- `semantic_subcategory_vtf_regional`
- `semantic_bigrams`
- `semantic_trigrams`
- `semantic_bigrams_detailed`
- `semantic_trigrams_detailed`
- `semantic_pmi`
- `semantic_pmi_detailed`
- `semantic_composition_patterns`
- `semantic_composition_patterns_detailed`
- `semantic_conflicts`
- `semantic_conflicts_detailed`

### N-Gram Analysis

- `ngram_frequency`
- `regional_ngram_frequency`
- `ngram_tendency`
- `ngram_significance`
- `structural_patterns`
- `regional_centroids`

### Morphology Patterns

- `pattern_frequency_global`
- `pattern_regional_analysis`

### Spatial And Clustering

- `spatial_hotspots`
- `spatial_clusters`
- `region_spatial_aggregates`
- `spatial_tendency_integration`
- `cluster_assignments`
- `cluster_profiles`
- `clustering_metrics`
- `region_similarity`

## Tables To Drop Or Skip

These are compact cleanup targets because they are village-level derived tables.

- `village_ngrams`
- `village_semantic_structure`
- `village_features`
- `village_spatial_features`
- `village_cluster_assignments`

The backend currently queries these tables in village-detail, village-search, and subset-compute paths. Compact mode must either disable those routes for compact databases or make those routes return a controlled degraded response when the tables are absent.

## Raw And Preprocessed Village Tables

These two tables need a staged treatment:

- `广东省自然村`
- `广东省自然村_预处理`

Initial compact implementation should keep them until a replacement regional metadata table exists, because some metadata and regional APIs still use them for counts, region lists, and region hierarchy.

Final compact implementation should replace their runtime role with a small regional catalog table:

- `region_catalog`

Suggested `region_catalog` columns:

- `region_level`
- `region_name`
- `city`
- `county`
- `township`
- `village_count`
- `centroid_lon`
- `centroid_lat`

After backend reads are migrated to `region_catalog`, compact mode can drop or omit `广东省自然村` and `广东省自然村_预处理`.

## National Profile Changes

Set Phase 1 in `config/pipeline.national.json` to keep compact character similarity:

```json
{
  "precompute_similarities": true,
  "top_k": 20
}
```

`top_k: 20` is the recommended national compact default because it preserves the character similarity and character network APIs while keeping the table bounded by `characters x 20`.

## Implementation Plan

### Task 1: Add Compact Retention Config

**Files:**

- Modify: `config/pipeline.national.json`
- Modify: `config/pipeline.guangdong.json`
- Modify: `src/pipeline_config.py`
- Test: `tests/unit/test_pipeline_config.py`

Steps:

- Add a top-level `retention` object to pipeline profiles.
- In Guangdong, set compact cleanup off.
- In national, set compact cleanup on and list the drop targets above.
- Test that national retention keeps `char_similarity` and drops only village-level derived tables.
- Commit with message: `feat: add compact retention profile config`.

### Task 2: Add Phase12 Village N-Gram Skip

**Files:**

- Modify: `scripts/core/phase12_ngram_analysis.py`
- Modify: `config/pipeline.national.json`
- Test: `tests/unit/test_phase12_configurable_generation.py`

Steps:

- Add a CLI option that can skip `step9_populate_village_ngrams`.
- Configure national compact to skip `village_ngrams`.
- Keep Guangdong behavior unchanged.
- Test that national Phase12 dry-run command emits the skip option.
- Commit with message: `feat: skip village ngrams in compact profile`.

### Task 3: Add Post-Phase Retention Cleanup

**Files:**

- Modify: `run_all_phases.py`
- Create: `src/pipeline_retention.py`
- Test: `tests/unit/test_pipeline_retention.py`

Steps:

- Implement a retention cleanup helper that drops configured tables only after successful selected phases.
- Refuse to drop tables not listed in the configured retention policy.
- Ensure `char_similarity` is never dropped by the national compact policy.
- Add a dry-run display listing keep/drop tables.
- Commit with message: `feat: apply compact retention cleanup`.

### Task 4: Backend Degraded Responses For Dropped Village Tables

**Files:**

- Modify: `api/village/data.py`
- Modify: `api/village/search.py`
- Modify: `api/compute/subset.py`
- Test: existing or new API unit tests under `tests/unit/`

Steps:

- Add table-existence checks before querying compact-dropped tables.
- For missing `village_ngrams`, return `ngrams: null` or an empty ngram payload instead of a SQL error.
- For missing `village_semantic_structure`, return `semantic_structure: null`.
- For missing `village_features`, return a controlled `404` or `422` explaining that the compact database does not support village-level features/subset compute.
- Commit with message: `feat: degrade village APIs for compact databases`.

### Task 5: Add Region Catalog Replacement

**Files:**

- Create: `scripts/core/build_region_catalog.py`
- Modify: `run_all_phases.py`
- Modify: `api/metadata/stats.py`
- Modify: relevant regional metadata reads after confirming exact call sites
- Test: `tests/unit/test_region_catalog.py`

Steps:

- Build `region_catalog` from preprocessed villages.
- Use it for region counts, hierarchy, and optional centroids.
- Keep raw/preprocessed tables until this migration is complete.
- Commit with message: `feat: add compact region catalog`.

### Task 6: Enable Final Raw/Preprocessed Cleanup

**Files:**

- Modify: `config/pipeline.national.json`
- Modify: `src/pipeline_retention.py`
- Test: `tests/unit/test_pipeline_retention.py`

Steps:

- Add a second-stage retention option for dropping `广东省自然村` and `广东省自然村_预处理`.
- Keep this disabled until `region_catalog` covers the required backend reads.
- Commit with message: `feat: support final compact raw table cleanup`.

## Verification Commands

Run after each implementation task:

```bash
.venv/bin/pytest tests/unit/test_pipeline_config.py tests/unit/test_phase_entrypoint_args.py -q
```

Run after Phase12 and retention changes:

```bash
.venv/bin/pytest tests/unit/test_phase12_configurable_generation.py tests/unit/test_phase12_ngram_cleanup.py tests/unit/test_pipeline_retention.py -q
```

Run before final handoff:

```bash
.venv/bin/python run_all_phases.py --config config/pipeline.national.json --phases 1,12,14 --skip-dependencies --dry-run
.venv/bin/python -m py_compile run_all_phases.py src/pipeline_config.py src/pipeline_retention.py scripts/core/phase12_ngram_analysis.py
```

## Confirmation Checklist

- `char_similarity` remains in compact mode.
- Village-level derived tables are cleanup targets.
- Raw and preprocessed village tables are not dropped in the first implementation.
- `region_catalog` is required before final raw/preprocessed cleanup.
- Guangdong full profile remains backward compatible.

